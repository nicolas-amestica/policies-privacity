# Recursos estáticos (`assets/`)

Coloque aquí los archivos binarios referenciados desde [`index.html`](../index.html).

| Archivo       | Uso                                      |
| ------------- | ---------------------------------------- |
| `gi-logo.png` | Logo en cabecera oscura del documento    |

Si cambia el nombre o la ruta del logo, actualice la etiqueta `<img>` correspondiente en `index.html`.
